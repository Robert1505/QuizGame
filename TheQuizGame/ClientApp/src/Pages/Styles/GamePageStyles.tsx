import React, { ReactElement } from 'react'

interface Props {
    
}

export default function GamePageStyles({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
